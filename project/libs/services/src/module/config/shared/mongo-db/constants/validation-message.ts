export enum ValidationMessage {
  MongoHostRequired = 'DB_HOST (Mongo) is required',
  MongoNameRequired = 'DB_NAME (Mongo) is required',
  MongoPortRequired = 'DB_PORT (Mongo) is required',
  MongoUserRequired = 'DB_USER (Mongo) is required',
  MongoPasswordRequired = 'DB_PASSWORD (Mongo) is required',
  MongoBaseAuthRequired = 'DB_AUTH_BASE (Mongo) is required',
}
